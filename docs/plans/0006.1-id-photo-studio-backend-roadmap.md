# ID Photo Studio Backend Roadmap & Checkpoints

<callout icon="♞">**Status:** Draft · **Owner:** Sam · **Date:** 2026-09-10</callout>

Implements the backend half of [0006-id-photo-studio-specification.md](0006-id-photo-studio-specification.md).
Frontend (`src/pages/crop.astro` plus React island) is explicitly deferred.

---

## Phase 0 — Docs (this change)

- [x] ADR [0008](../decisions/0008-crop-thin-backend-client-removal.md) (`Proposed`).
- [x] Spec `0006` (`Draft`).
- [x] This roadmap `0006.1` (`Draft`).
- [x] Index rows in `decisions/README.md` and `plans/README.md`.
- [x] `Changelog.md` Unreleased entry (docs-only).
- **Checkpoint**: `node scripts/check-links.js` passes.

## Phase 1 — Canonical lib

- [x] Created `src/lib/crop/presets.ts` (presets, backgrounds, export caps).
- [x] Created `src/lib/crop/validation.ts` (pure validators).
- **Checkpoint**: `format`, `lint`, `check` pass; no new dependencies.

## Phase 2 — API routes

- [x] `src/pages/api/crop/config.ts` — `GET`, CDN-cached, imports presets.
- [x] `src/pages/api/crop/usage.ts` — `POST` metadata-only, KV rate-limit
      (production only), `no-store` headers.
- [x] `src/pages/api/crop/remove.ts` — `POST` 501 stub with
      `fallback: "client"`.
- **Checkpoint**: `build` passes; routes follow `contact.ts` error shape.

## Phase 3 — Lab content entry

- [x] `src/content/experiments/id-photo-studio.md` (`status: active`,
      model-download warning, demo link to `/crop`).
- [x] Left `src/pages/experiments/index.astro` paused until frontend lands.
- **Checkpoint**: `check` (content schema) passes; build renders
  `/experiments/id-photo-studio/`.

## Phase 4 — Verify

- [x] New `tests/e2e/crop-api.spec.ts` (config presets, remove 501) —
      20 passed across 5 browser projects.
- [x] Full suite in order: `format`, `lint`, `check` (0 errors),
      `build` (21s, `/experiments/id-photo-studio/` rendered),
      `test:e2e` crop spec (20/20), `test:a11y` (40/40).
- **Done criteria**: all green, no `wrangler.jsonc` diff, no image
  persistence anywhere.
